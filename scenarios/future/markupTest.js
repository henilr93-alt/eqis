// Agent markup addition + margin check
// Tests the B2B markup/margin feature where agents add their
// commission to base fares before sharing quotes with end clients.

const scenarios = []; // TO BE POPULATED

module.exports = { scenarios };
