// Browser back button navigation test
// Uses browser back button throughout the booking journey.
// Verifies data persistence, form state, and correct page rendering on back.

const scenarios = []; // TO BE POPULATED

module.exports = { scenarios };
