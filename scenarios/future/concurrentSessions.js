// Concurrent sessions test
// Opens the same booking flow in two browser tabs simultaneously.
// Tests for race conditions, session conflicts, and data cross-contamination.

const scenarios = []; // TO BE POPULATED

module.exports = { scenarios };
